/** @jsx jsx */
import React, { FC, useState, useCallback, Fragment } from "react";
import { jsx, css } from "@emotion/core";
import UserPoints from '../../templates/userpoints/userpoints'

const TopPage: FC = () => <UserPoints />

export default TopPage;
